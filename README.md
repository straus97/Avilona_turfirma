# Avilona_turfirma

Веб-сайт и внутренняя система бронирования туристического агентства «Авилона».
Приложение построено на Laravel 10 и обслуживает три основные роли: **турист**, **менеджер**, **администратор**.

## Текущее состояние

- Функциональный checkpoint, на основе которого актуализирована эта документация: `61975e0d` — `test: cover message polling isolation`.
- Предыдущий документационный коммит: `e70722ba` — `docs: refresh project state and roadmap` (база: `ddad32d9`).
- Фактический текущий HEAD всегда определяется Git (`git rev-parse HEAD`), а не значением, зафиксированным в этом файле.
- После `e70722ba` было выполнено семь коммитов, полностью завершивших Этап 5 (жизненный цикл заявки) и Этап 6 (чат туриста и менеджера). Этот slice — актуализация документации под эти изменения; полный список коммитов и возможностей см. в [docs/roadmap.md](docs/roadmap.md).
- Канонический статус: этапы 0–4 завершены исторически; Этап 5 (жизненный цикл заявки) — ✅ завершён; Этап 6 (чат туриста и менеджера) — ✅ завершён (см. «Основные возможности» и `docs/roadmap.md`).
- Продуктовая готовность: не подтверждена в целом — применяйте только как характеристику отдельных функций.

## Основные возможности

Подтверждённые реализованные области:

- Аутентификация и роли (admin / manager / tourist)
- Подписанная верификация email
- Бронирования: создание, просмотр, привязка к менеджеру
- Личные документы в приватном хранилище с защитой по роли
- Документы бронирования (BookingDocument) в приватном хранилище
- Защищённые пути скачивания с проверкой владельца и роли
- UI управления BookingDocument для менеджера и администратора
- Доступ туриста к документам бронирования (только чтение, через отдельный маршрут)
- **Этап 5. Жизненный цикл заявки (✅ завершён):** авторизация действий над бронированием, правила и UI перехода статусов, владение и атомарность при создании, авторизация назначения активных менеджеров/администраторов, назначенные администраторы управляют бронированием наравне с менеджерами, сохранение переназначения с уведомлением, защищённая от сбоя доставки post-persistence нотификация при первичном назначении и переназначении. Полный список коммитов — в `docs/roadmap.md`.
- **Этап 6. Чат туриста и менеджера (✅ завершён):** авторизация участников и получателей, серверная проверка получателя, правила доступа владельца/текущего исполнителя/администратора, отзыв доступа у прежнего исполнителя, приватное хранение вложений, защищённое скачивание вложений, сообщения только с вложением, удаление сохранённого приватного вложения при сбое создания сообщения, ролевые ссылки в email, изоляция и авторизация опроса (polling) по бронированию, детерминированный порядок сообщений, пометка прочитанными только сообщений, адресованных опрашивающему пользователю, отсутствие прямых путей к приватным вложениям. Полный список коммитов — в `docs/roadmap.md`.

## Технологический стек

| Слой | Технология |
|---|---|
| PHP | 8.3.32 |
| Фреймворк | Laravel 10.48.10 |
| Зависимости PHP | Composer 2.8.3 |
| Node.js | 22.11.0 |
| npm | 11.12.1 |
| Сборка фронтенда | Vite 4.x |
| UI-слой | Blade + Bootstrap 5 (основной) |
| База данных | MySQL, канонический экземпляр `turfirma_rebuild_v4` |
| Тесты | PHPUnit 10, SQLite `:memory:` |

> TailwindCSS и Alpine.js присутствуют в `package.json`, но их фактическое использование в текущем коде нужно проверять в каждом новом слое.

## Локальная разработка

```bash
composer install
npm ci
```

Создайте и настройте локальный `.env` в соответствии с параметрами текущего окружения.
**Не вставляйте в `.env` данные продакшн-сервера и не публикуйте этот файл.**

```bash
php artisan serve --host=127.0.0.1 --port=8000
npm run dev        # режим разработки с HMR
# или
npm run build      # сборка для продакшн
```

> **Важно:** не запускайте команды записи в базу данных (миграции, импорты, сидеры) без отдельного проверенного плана. Канонические данные `turfirma_rebuild_v4` уже в полностью согласованном состоянии (52 миграции, 0 ожидающих).

## Безопасность данных

- Не публикуйте `.env`, SQL-дампы, токены, куки и личные документы пользователей.
- Приватные файлы хранятся вне `public/` и отдаются только через авторизованные маршруты.
- Не добавляйте `vendor/` и `node_modules/` в репозиторий.

## Тестирование

PHPUnit использует SQLite `:memory:` — **не должен** подключаться к MySQL и к канонической базе `turfirma_rebuild_v4`.

Текущий baseline: **270 тестов, 836 утверждений**.

```bash
php artisan test --no-ansi
```

Известное предупреждение: устаревшая XML-схема в `phpunit.xml` — не является новой ошибкой, исправляется в отдельном слое.

## Документация

- [docs/README.md](docs/README.md) — индекс документации и руководство по источникам истины
- [docs/roadmap.md](docs/roadmap.md) — дорожная карта проекта

Файлы в папке `docs/` могут содержать устаревшие утверждения о статусе задач, дедлайнах и командах.
Актуальное состояние определяет `docs/README.md`.

## Текущий приоритет

Этап 5 (жизненный цикл заявки) завершён на checkpoint `e71d8cda`. Этап 6 (чат туриста и менеджера) завершён на checkpoint `61975e0d`. `61975e0d` — также общий функциональный checkpoint, лежащий в основе полной актуализации документации. Текущий слой — актуализация документации и подготовка внешнего handoff/архива под эти изменения.
Этап 7 начнётся только после того, как этот документационный коммит будет запушен, реальный HEAD на GitHub подтверждён, и для него будут созданы свежий внешний handoff и sanitized source archive.
